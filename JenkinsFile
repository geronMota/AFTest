pipeline {
    agent any
    stages {
        stage('Olá Mundo') {
            steps {
                echo 'Jenkinsfile encontrado! Projeto AFTest rodando.'
                bat 'mvn --version'  // testa se Maven está ok
            }
        }
    }
}